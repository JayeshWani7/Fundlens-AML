
  # Design fund flow graph

  This is a code bundle for Design fund flow graph. The original project is available at https://www.figma.com/design/xQvWAzG7tByzT0AoXQyWSv/Design-fund-flow-graph.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  